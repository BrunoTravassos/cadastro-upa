import { NomePacienteDirective } from './nome-paciente.directive';

describe('NomePacienteDirective', () => {
  it('should create an instance', () => {
    const directive = new NomePacienteDirective();
    expect(directive).toBeTruthy();
  });
});
